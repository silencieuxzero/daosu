---
import Layout from '../layouts/Layout.astro';
import { config } from '../lib/config';

const githubHref = config.profile.github.startsWith('http')
	? config.profile.github
	: `https://${config.profile.github}`;
---

<Layout title=`关于 · ${config.site.name}`>
	<main class="page">
		<article class="prose">
			<header class="about-head">
				<p class="kicker">关于</p>
				<h1>关于这个网站</h1>
			</header>

			<p>
				这是一个粉丝制作向的悼溯茶馆网站，网站内主要存放：<strong>设定</strong>与<strong>故事</strong>——
				正是这些基础文档构造了一个丰富的世界。
			</p>

			<h2>关于悼溯</h2>
			<ul>
				<li>一个原创的世界观企划</li>
				<li>前身为国人AU《命定之下》</li>
			</ul>

			<h2>联系</h2>
			<p>
				可以通过下面的方式找到我：
			</p>
			<ul>
				<li>邮箱：<a href={`mailto:${config.profile.email}`}>{config.profile.email}</a></li>
				<li>GitHub：<a href={githubHref} rel="nofollow">{config.profile.github}</a></li>
			</ul>
		</article>
	</main>
</Layout>

<style>
	.page {
		padding: clamp(40px, 6vh, 72px) clamp(16px, 4vw, 48px);
		max-width: 720px;
		margin: 0 auto;
	}

	.about-head {
		position: relative;
		padding-bottom: 26px;
		margin-bottom: 10px;
		border-bottom: var(--line) solid var(--border);
		background-color: var(--bg);
		background-image: var(--texture-rect);
		background-size: 56px 56px;
	}

	.about-head h1 {
		margin-top: 14px;
		font-size: clamp(28px, 4.5vw, 42px);
		font-weight: 900;
	}
</style>

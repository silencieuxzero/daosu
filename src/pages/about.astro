---
import Layout from '../layouts/Layout.astro';
import { config } from '../lib/config';

const githubHref = config.profile.github.startsWith('http')
	? config.profile.github
	: `https://${config.profile.github}`;
---

<Layout title="关于 · DAOSU">
	<main class="page">
		<article class="prose">
			<header class="about-head">
				<p class="kicker">关于</p>
				<h1>关于这个博客</h1>
			</header>

			<p>
				这是 DAOSU 的个人博客。内容围绕两件事：<strong>设计</strong>与<strong>先秦</strong>——
				或者说，是现代平面主义与古代纹样之间的一种对照。
			</p>

			<h2>为什么是这两种主题</h2>
			<p>
				现代主题是黑白灰：直角、粗边框、纯色填充，没有阴影，信息直接裸露。
				古代主题是黛蓝（<code>#2E3B52</code>）：云雷纹铺底、兽面纹缀脚、铭文做水印。
				两套气质共用同一套 CSS 变量，切换主题只是改几个值。
			</p>

			<h2>关于我</h2>
			<ul>
				<li>平面设计爱好者，Swiss 风格的追随者</li>
				<li>对商周青铜器纹样有持续的兴趣</li>
				<li>偶尔写代码，主要是 Web 前端</li>
			</ul>

			<h2>联系</h2>
			<p>
				可以通过下面的方式找到我：
			</p>
			<ul>
				<li>邮箱：<a href={`mailto:${config.profile.email}`}>{config.profile.email}</a></li>
				<li>GitHub：<a href={githubHref} rel="nofollow">{config.profile.github}</a></li>
			</ul>
		</article>
	</main>
</Layout>

<style>
	.page {
		padding: clamp(40px, 6vh, 72px) clamp(16px, 4vw, 48px);
		max-width: 720px;
		margin: 0 auto;
	}

	.about-head {
		position: relative;
		padding-bottom: 26px;
		margin-bottom: 10px;
		border-bottom: var(--line) solid var(--border);
		background-color: var(--bg);
		background-image: var(--texture-rect);
		background-size: 56px 56px;
	}

	.about-head h1 {
		margin-top: 14px;
		font-size: clamp(28px, 4.5vw, 42px);
		font-weight: 900;
	}
</style>

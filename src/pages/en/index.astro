---
// Home (English)
import Layout from '../../layouts/Layout.astro';
import { config } from '../../lib/config';
import BlogHome from '../../components/BlogHome.astro';
---

<Layout title=`${config.site.name} · Personal Blog`>
	<BlogHome locale="en" />
</Layout>
